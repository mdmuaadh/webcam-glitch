# Webcam Mirror Glitch

This project displays a live mirrored webcam feed and applies a glitch effect after a short delay. It uses browser camera access to capture real-time video. Make sure to allow camera permissions and run on localhost or HTTPS.
